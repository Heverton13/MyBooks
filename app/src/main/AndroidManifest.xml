<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mybooks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bookicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bookicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">
        <activity android:name=".activity.ListBookGrid"></activity>
        <activity android:name=".activity.PageViewBooks" />
        <activity android:name=".activity.ListBooksRecycle" />
        <activity android:name=".activity.SearchBook" />
        <activity android:name=".activity.TelaDeListaBookAdaptar" />
        <activity android:name=".activity.ListBooks" />
        <activity android:name=".activity.AddBook" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>